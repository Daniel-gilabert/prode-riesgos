import React from 'react';

export default function RolesPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Roles (Admin)</h1>
      <p>This is a placeholder UI for managing roles per tenant. Connect to the RBAC backend to list and modify roles.</p>
    </div>
  );
}
