import React from 'react';

export default function TenantsPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Tenants</h1>
      <p>Tenant management placeholder. Integrate with admin RBAC endpoints to list, create, and configure tenants.</p>
    </div>
  );
}
